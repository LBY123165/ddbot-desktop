# DDBOT-WSa Desktop

一个用于管理 DDBOT-WSa 的桌面端 WebUI，基于 Tauri v2 + Rust 后端 + Vue3 前端构建。

## 项目状态

### ✅ 已完成

1. **后端 (Rust/Tauri)**
   - [x] 进程管理（启动/停止/重启）
   - [x] Admin API 配置和 Token 管理
   - [x] 现有部署导入（排除原 exe）
   - [x] 自动下载和安装最新版本
   - [x] 用户授权机制（approved.json）

2. **前端 (Vue3)**
   - [x] Pinia 状态管理
   - [x] Vue Router 路由配置
   - [x] 侧边导航栏
   - [x] 首页状态概览卡片
   - [x] 首次运行设置向导
   - [x] 设置页面
   - [x] 响应式布局

3. **UI 组件**
   - [x] Button 组件
   - [x] StatusCard 组件
   - [x] Navigation 组件
   - [x] 全局样式

### 🚧 待完成

1. **Admin API 真实调用**
   - [ ] 实现 `onebot_status_text()` 调用 `/api/v1/onebot/status`
   - [ ] 实现 `subs_summary_text()` 调用 `/api/v1/subs/summary`
   - [ ] 添加 Bearer Token 鉴权

2. **高级功能**
   - [ ] 配置文件编辑器
   - [ ] 日志查看器
   - [ ] 模板中心
   - [ ] 扫码 Cookie 功能

3. **离线 DB 读取**
   - [ ] 实现 `.lsp.db` 只读方案
   - [ ] 文件锁机制

## 项目结构

```
DDBOT-WSa-Desktop/
├── src/
│   ├── components/          # Vue 组件
│   │   ├── Button.vue
│   │   ├── NavigationPage.vue
│   │   └── StatusCard.vue
│   ├── pages/              # 页面组件
│   │   ├── HomePage.vue
│   │   ├── SettingsPage.vue
│   │   └── SetupWizard.vue
│   ├── router/             # 路由配置
│   │   └── index.ts
│   ├── stores/             # Pinia 状态管理
│   │   └── app.ts
│   ├── App.vue             # 根组件
│   ├── main.ts             # 入口文件
│   └── style.css           # 全局样式
├── src-tauri/
│   ├── src/
│   │   ├── main.rs         # Tauri 主程序
│   │   ├── ddbot.rs        # DDBOT 相关逻辑
│   │   └── process.rs      # 进程管理
│   └── Cargo.toml
└── package.json
```

## 开发指南

### 前置要求

- Node.js 18+
- Rust 1.70+
- Yarn 或 npm

### 安装依赖

```bash
yarn install
```

### 开发模式

```bash
yarn tauri:dev
```

### 构建

```bash
yarn build
```

## UI 设计

本项目参考了 [OpenList-Desktop](https://github.com/OpenListTeam/openlist-desktop) 的设计风格：

- 深色主题
- 渐变背景
- 玻璃拟态效果
- Lucide 图标
- 响应式布局

## 核心功能

### 1. 首次运行向导

首次启动时会引导用户：
- 选择是否已有 DDBOT-WSa 部署
- 导入现有部署或全新安装
- 授权 Admin API 访问

### 2. 主页概览

显示：
- DDBOT-WSa 进程状态
- OneBot 连接状态
- 订阅统计信息
- 当前版本信息
- 快速操作按钮

### 3. 设置页面

配置：
- 安装通道
- 路径信息
- 管理工作目录

## 数据隔离

Desktop 始终在自己的数据目录中运行：
- 工作目录：`%AppData%/DDBOT-WSa-Desktop/`
- 数据目录：`workdir()/data/`
- 管理目录：`data_dir()/ddbot/`

不会破坏用户原有的部署目录。

## 技术栈

- **后端**: Tauri v2, Rust
- **前端**: Vue 3, TypeScript
- **状态管理**: Pinia
- **路由**: Vue Router
- **UI**: 自定义组件（参考 OpenList 风格）
- **图标**: Lucide Vue Next

## 许可证

与 DDBOT-WSa 项目保持一致。
